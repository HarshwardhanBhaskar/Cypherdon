server:
  port: 8080

spring:
  application:
    name: cypherdon-core
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SMTP_USERNAME:yashth12coc@gmail.com}
    password: ${SMTP_PASSWORD:YOUR_16_DIGIT_APP_PASSWORD_HERE}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres}
    username: ${DATABASE_USERNAME:postgres.oanjytlckveeuyyaslpr}
    password: ${DATABASE_PASSWORD:Harsh@wardhan987}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 25
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 5000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false # Disable in production to reduce log noise

# JWT Configuration for Supabase Auth
jwt:
  secret: ${JWT_SECRET:c3XNOpcFsRGr1r4ROJjtq3MFrEknGqI0EGYjFvb7H+shWqxjLdRYvclNgEtP6jHLRlIxdo2CZpdyqQPAtmXBNw==}

# Internal service-to-service authentication
cypherdon:
  internal:
    secret: ${INTERNAL_SERVICE_KEY:cypherdon_internal_123}
  fastapi:
    url: ${FASTAPI_URL:http://localhost:8000}

# Observability - Expose Prometheus Metrics
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  metrics:
    tags:
      application: ${spring.application.name}

# Resilience4j Circuit Breaker and Retry Configuration
resilience4j.circuitbreaker:
  instances:
    aiEngine:
      slidingWindowSize: 10
      failureRateThreshold: 50
      waitDurationInOpenState: 15s
      permittedNumberOfCallsInHalfOpenState: 3

resilience4j.retry:
  instances:
    aiEngine:
      maxAttempts: 3
      waitDuration: 2s
